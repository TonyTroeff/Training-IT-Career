@model ReusableForm<TaskItemInputModel>

<form asp-action="@Model.Action">
    <partial name="TaskItems/_Inputs" model="Model.Data" />
    <div class="form-group">
        <input type="submit" value="@Model.Button" class="btn btn-primary" />
    </div>
</form>